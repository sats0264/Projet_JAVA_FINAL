package model;

public class Gerant extends User {

	public Gerant(String username, String password, TypeUser role) {
		super(username, password, role);
	}
    // Additional attributes and methods specific to GymManager
}